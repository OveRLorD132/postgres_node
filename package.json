{
  "name": "test",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "npx tsc --build tsconfig.json && node --env-file=.env ./dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^22.5.4",
    "typescript": "^5.6.2"
  }
}
